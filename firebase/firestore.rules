rules_version = '2';

// Reglas permisivas para emuladores locales.
// NO usar en producción.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
