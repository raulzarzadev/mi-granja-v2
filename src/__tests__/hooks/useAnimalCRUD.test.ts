/**
 * Test básico para useAnimalCRUD hook
 */
describe('useAnimalCRUD', () => {
  it('should work', () => {
    // Simple test para verificar que jest funciona
    const result = 1 + 1
    if (result !== 2) {
      throw new Error('Math is broken')
    }
  })
})
