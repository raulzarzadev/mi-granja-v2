/**
 * Test para BreedingCard component
 */

describe('BreedingCard', () => {
  it('should work', () => {
    // Test básico para verificar que el test runner funciona
    const result = 1 + 1
    if (result !== 2) {
      throw new Error('Math is broken')
    }
  })
})
